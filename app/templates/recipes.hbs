{{page-title "Recipes"}}
<h1>Recipe Book</h1>
{{#each @model as |recipe|}}
  <RecipeShortOverview
    @title={{recipe.title}}
    @description={{recipe.description}}
    @id={{recipe.id}}
  > {{yield}}</RecipeShortOverview>
{{/each}}